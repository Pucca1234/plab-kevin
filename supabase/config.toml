project_id = "waaaievbqzwdmajaougf"

[api]
enabled = true
schemas = ["public", "graphql_public", "bigquery"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[auth]
enabled = true
site_url = "https://social-match-dashboard-mvp.vercel.app"
additional_redirect_urls = [
  "http://localhost:3000/**",
  "https://social-match-dashboard-mvp.vercel.app/**",
  "https://plab-kevin.vercel.app/**",
  "https://*-puccas-projects-a3c13623.vercel.app/**"
]
jwt_expiry = 3600

[auth.mfa]
max_enrolled_factors = 10

[auth.mfa.totp]
enroll_enabled = true
verify_enabled = true

[auth.mfa.phone]
enroll_enabled = false
verify_enabled = false

[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = true
secure_password_change = false
max_frequency = "1m0s"
otp_length = 8
otp_expiry = 3600
